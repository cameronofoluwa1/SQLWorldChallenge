1. select * from actor;
2. select last_name from actor where first_name = "John";
3. select * from actor where last_name = "Neeson";
4. select * from actor where actor_id like '%0';
5. select * from film where film_id = 100;
6. select * from film where rating = "R";
7. select * from film where rating != "R";
8. select * from film order by length asc limit 10;
9. select * from film order by length desc;
10. select * from film where special_features like '%Deleted Scenes%';
11. SELECT last_name FROM actor group by last_name desc having count(last_name) = 1;
12. SELECT last_name FROM actor group by last_name having count(last_name) >= 2 order by count(last_name) desc;
13. select a.first_name, fa.actor_id, count(fa.actor_id) from film_actor fa join actor a ON fa.actor_id=a.actor_id group by fa.actor_id order by count(fa.actor_id) desc;
14. SELECT DATE_ADD(rental_date, INTERVAL 6 DAY) as "New Return Date" from rental where inventory_id between 1 and 8 and return_date is null;
15. select avg(length) from film;
16. SELECT avg(f.length), fc.category_id FROM film_category fc JOIN film f ON fc.film_id=f.film_id group by fc.category_id;
17. select * from film where description like '%robot%';
18. select * from film where release_year = 2010;
19. SELECT f.title, fc.category_id  FROM film_category fc  JOIN film f ON fc.film_id=f.film_id  where fc.category_id = 11;
20. select first_name, last_name from staff where staff_id = 2;
21. SELECT fa.film_id, f.film_id, f.title, fa.actor_id FROM film_actor fa  JOIN film f ON fa.film_id=f.film_id   where fa.actor_id = 16;
22. select count(distinct country) from country;
23. select name from language order by name desc;
24. select first_name, last_name from actor where last_name like '%son' order by first_name asc;
25. select count(film_id), category_id from film_category group by category_id order by category_id;
